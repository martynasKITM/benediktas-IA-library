package com.benedict.library.Controllers;

import javafx.fxml.Initializable;

import java.net.URL;
import java.util.ResourceBundle;

/**
 * Controller class for the "Dashboard" view.
 * This class handles the interactions and logic related to the dashboard view in the application.
 *
 * Implements the `Initializable` interface to perform any necessary setup logic
 * when the corresponding FXML file is loaded.
 */
public class DashboardController implements Initializable {

    /**
     * Called to initialize the controller after the FXML file has been loaded.
     * This method is used for setting up any necessary components or logic
     * for the dashboard view.
     *
     * @param location  the location used to resolve relative paths for the root object, or null if not applicable.
     * @param resources the resources used to localize the root object, or null if not applicable.
     */
    @Override
    public void initialize(URL location, ResourceBundle resources) {
        // Initialization logic can be added here, if needed in the future.
    }
}
