package com.benedict.library.Controllers;

import javafx.fxml.Initializable;

import java.net.URL;
import java.util.ResourceBundle;

/**
 * Controller class for the "Add Income" functionality.
 * This class is responsible for handling the interactions and logic related to
 * the "Add Income" view in the application.
 *
 * Implements the `Initializable` interface to perform any necessary initialization
 * when the corresponding FXML view is loaded.
 */
public class AddIncomeController implements Initializable {

    /**
     * Called to initialize the controller after the FXML file has been loaded.
     *
     * @param location  the location used to resolve relative paths for the root object, or null if not applicable.
     * @param resources the resources used to localize the root object, or null if not applicable.
     */
    @Override
    public void initialize(URL location, ResourceBundle resources) {
        // Initialization logic can be added here.
    }
}
